name: Sphinx documentation

on:
  push:
    branches:
      - noetic-devel
      - feature/sphinx-docs

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true

      - name: Generate Sphinx documentation
        uses: ammaraskar/sphinx-action@master
        with:
          pre-build-command: "pip install sphinx_rtd_theme"
          docs-folder: "docs/sphinx/source"
          build-command: "sphinx-build -b html . ../build"

      - name: Deploy Github Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: "docs/sphinx/build"

      # - name: Commit documentation changes
      #   run: |
      #     git clone https://github.com/hrii-iit/xsens_awinda_ros.git --branch gh-pages --single-branch gh-pages
      #     cp -r docs/sphinx/build/* gh-pages/
      #     cd gh-pages
      #     git config --local user.email "action@github.com"
      #     git config --local user.name "GitHub Action"
      #     git add .
      #     git commit -m "Update documentation" -a || true

      # - name: Push changes
      #   uses: ad-m/github-push-action@master
      #   with:
      #     branch: gh-pages
      #     directory: gh-pages
      #     github_token: ${{ secrets.GITHUB_TOKEN }}